# HomeWork_6_3

ACC(CACA)A (AA)DD(BD)(AB) DDCAC D==D==D

ADC(CACA)A (AA)DD(BD)(AB) DCBAC DDD

(D)02.下列关于图的说法中，错误的是()
I.对一个无向图进行深度优先遍历时，得到的深度优先遍历序列是唯一的
II.若有向图不存在回路，即使不用访问标志位，同一顶点也不会被访问两次
III.采用深度优先遍历或拓扑排序算法可以判断一个有向图中是否有环(回路)
IV.对任何非强连通图必须2次或以上调用广度优先遍历算法才可访问所有的顶点

A.I,II,III B.II,III C.I,II D.I,II,IV

>   错选:C,I错在序列的唯一性由存储方式决定;II错在有向图有环就会循环;IV感觉是对的,因为调用BFS和DFS都只能得到一个连通分量(强连通分量)
>
>   对任何非强连通图$A \rightarrow B$,一次足矣,所以IV也是错的;
>   II的错误在于如果是dfs,本身就会回退,都不需要环

(C)12.判断有向图中是否存在回路，除拓扑排序外，还可利用()。(注:涉及下节内容)
A.求关键路径的方法
B.求最短路径的Dijkstra算法
C.深度优先遍历算法
D.广度优先遍历算法

>   错选:D,当时候学的时候就说可以用BFS来看环和回路为什么不对?
>
>   BFS在无向图里面最简单,但是有向图里面会有bug(tail访问head以后,head又自己把visited标记了一遍)

(B)13.设无向图G=(V,E)和G'=(V,E)，若G是G的生成树，则下列说法错误的是()。

A.G'为G的子图
B.G'为G的连通分量
C.G'为G的极小连通子图且V=V'
D.G'是G的一个无环子图

>   错选:C
>
>   C:包含了所有的顶点,并且边数最少,所以C是对的
>
>   B:连通分量=极大连通子图,和生成树完全相反
>
>   极大连通子图=连通分量:目的是多占点,所以能到的都圈了
>
>   极小连通子图=生成树:目的是删除多余的边,只剩n-1条边

(D)17.[2015统考真题】设有向图G=(V,E)，顶点集$V=\{V_0,V_1,V_2,V_3\}$,边集$E=\{<v_0,v_1>,<v_0,v_2>,<v_0,v_3>,<v_1,v_3>\}$若从顶点$V_0$开始对图进行深度优先遍历，则可能得到的不同遍历序列个数是()。
A.2 B.3 C.4 D.5

>   RE:B(少看了一条边)
>
>   ```mermaid
>   ---
>   title: 有向图G1
>   ---
>   %%{init: {'flowchart': {'curve': 'basis', 'nodeSpacing': 60, 'rankSpacing': 60}}}%%
>   flowchart LR
>       classDef default fill:#fff,stroke:#333,stroke-width:2px,color:#000;
>       V0((V0))
>       V1((V1))
>   	V2((V2))
>   	V3((V3))
>   	V1-->V3
>   	V0-->V1
>   	V0-->V2
>   	V0-->V3
>   	
>   ```
>
>   {0,2,1,3}{0,2,3,1}{0,1,3,2}{0,3,2,1}{0,1,2,3}